// import { onSnapshot, doc } from "firebase/firestore";

// onSnapshot(doc(db, "jobs", jobId), (snapshot) => {
//   const data = snapshot.data();
//   if (data.status === "done") {
//     // inject video into shorts queue
//   }
// });